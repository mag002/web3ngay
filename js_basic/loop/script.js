// Loops in JavaScript are used to execute a block of statements repeatedly until a particular condition is satisfied.
// There are three types of loops in JavaScript:

// For loop: For loop is used when you know the number of iterations in advance. It has the following syntax:

for (let i = initialization; i < condition; i += increment / decrement) {
  // body of for loop
}
